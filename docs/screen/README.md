# Screenshots

Drop PNG captures of the running app here, using these exact filenames — the main README's
"Screens" section renders them (mirroring the arcana-windows layout).

| Filename | Screen |
|---|---|
| `Login.png` | Sign-in gate (admin / admin) |
| `Home.png` | Landing dashboard |
| `Orders.png` | Order master-detail (list + editable detail) |
| `Orders-Detail.png` | An order's detail with line items + totals (optional) |
| `Customers.png` | Customer master-detail editor |
| `Products.png` | Product master-detail editor |
| `Users-Roles.png` | Identity / RBAC admin |
| `Sync.png` | CRDT sync screen |
| `Reports.png` | Reports dashboard |
| `Plugins.png` | Loaded plugins |

## How to capture (no permissions needed)

1. Launch: `open /Users/jrjohn/Documents/projects/arcana-macos/.build/Arcana.app`
2. Sign in with **admin / admin**.
3. For each screen: **⌘⇧4 → Space → click the window** (captures just that window with a shadow).
4. Rename the resulting `~/Desktop/Screenshot ….png` to the filename above and move it here.

Then tell the assistant (or run it yourself) to wire them into the README.
